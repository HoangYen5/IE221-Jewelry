services:
  backend:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: jewelry_backend
    ports:
      - "8080:8080"
    environment:
      - WATCHFILES_FORCE_POLLING=1
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads:z
